# prepare raspberry environment for development or usage
# to invoke setup.sh make sure to give file permission to execute
# chmod +x setup.sh

echo -e "\n RUNNING >>> sudo apt-get update"
sudo apt-get update

echo -e "\n RUNNING >>> sudo apt-get install unzip"
sudo apt-get install unzip